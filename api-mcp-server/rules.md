# API MCP Server Rules

- Use `@7router/api-clients` where possible.
- Do not store secrets in scripts.
- Read API base URL and token from environment variables when overriding defaults.
- Keep smoke tests focused on end-to-end API behavior.

