# 7router API reverse proxy — 7router-api.minfect.com -> NestJS on 127.0.0.1:20131
# TLS: shared Cloudflare Origin Certificate for *.minfect.com (domain must be
# Cloudflare-proxied / orange cloud, SSL mode "Full (strict)"). No certbot.
#
# Uses the wildcard origin cert already on this host (covers 7router-api.minfect.com):
#   /etc/nginx/ssl/minfect.com.pem   (Origin Certificate, *.minfect.com)
#   /etc/nginx/ssl/minfect.com.key   (Origin Private Key)

server {
    listen 80;
    listen [::]:80;
    server_name 7router-api.minfect.com;
    # Cloudflare sends traffic over 443; redirect any direct HTTP hit.
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name 7router-api.minfect.com;

    ssl_certificate     /etc/nginx/ssl/minfect.com.pem;
    ssl_certificate_key /etc/nginx/ssl/minfect.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    # Matches the app's 200mb body limit for file uploads.
    # NOTE: Cloudflare's own upload cap still applies (100 MB on Free/Pro).
    client_max_body_size 200m;

    location / {
        proxy_pass http://127.0.0.1:20131;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Generous timeouts for large uploads.
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_set_header Connection "";
    }
}
