# UI

React + Vite web UI for `7router`.

- Token login stores the access token in browser local storage.
- API calls use `@7router/api-clients`.
- Pages live in `src/pages`.
- Client construction lives in `src/api/client.ts`.

Main pages: dashboard, providers, accounts, browser, manual sync, synced files, and settings.

