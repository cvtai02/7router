# UI Rules

- Use `@7router/api-clients` for backend calls.
- Store the app access token locally and send it as a bearer token.
- Do not show stored provider secrets after submission.
- Keep UI routes aligned with backend API contracts.
- Keep pages focused on real workflows, not landing-page content.

